[project]
name = "level_02_prompt_engineering"
version = "0.1.0"
description = "Minimal chatbot MVP — consider prompt engineering, with memory, with rewriter, no external data"
requires-python = ">=3.11"
dependencies = [
    "openai",
    "pydantic",
    "pyyaml",
    "pyreadline3",
]